import 'mocha';
import { expect } from 'chai';

describe('unit testing utils', () => {
  it('this is just for unit testing stuff so probably do tests for this file', () => {
    expect(1).to.equal(1);
  });
});
