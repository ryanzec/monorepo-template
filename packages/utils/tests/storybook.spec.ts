import 'mocha';
import { expect } from 'chai';

describe('storybook utils', () => {
  it('since this is just for storybook, probably will not do test here any time soon', () => {
    expect(1).to.equal(1);
  });
});
