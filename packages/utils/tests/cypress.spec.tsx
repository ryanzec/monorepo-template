import 'mocha';
import { expect } from 'chai';

describe('cypress utils', () => {
  it('this is just for cypress testing stuff so probably do tests for this file', () => {
    expect(1).to.equal(1);
  });
});
