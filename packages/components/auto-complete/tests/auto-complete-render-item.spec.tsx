import 'mocha';
import { expect } from 'chai';

describe('auto complete render item component', () => {
  it('does not have any unit level testable code', () => {
    expect(1).to.equal(1);
  });
});
