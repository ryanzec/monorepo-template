import { style } from '@vanilla-extract/css';

export const applicationLoadingCss = {
  Container: style({
    height: '100vh',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  }),
};
